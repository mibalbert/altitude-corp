/**
 * compoents/admin-sidenav/folders-and-files/folder-data.jsx
 */

import React from "react";

const FolderData = ({ parentFolderId, title }) => {
  // const posts  = await

  return <div>{title}</div>;
};

export default FolderData;
